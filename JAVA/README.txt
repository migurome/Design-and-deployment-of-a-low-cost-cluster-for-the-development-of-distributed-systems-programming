Para poder ejecutar los .jar correctamente debemos tener nuestra version de java en la versión 1.8, esto se configura de la siguiente manera:

1. sudo update-alternatives --config java
2. Elegiremos la opción 2

Para ejecutarlos escribiremos:
java -jar programa.jar